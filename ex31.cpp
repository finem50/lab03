#include <iostream>
#include <cmath>
using namespace std;

int main(void){

  //Compute 2^4
  int a=2, b=4;
  double p=pow(a,b);

  cout << a << " to the power of " << b << " is = " << p << endl;

  //Compute 3^3
  a=3, b=3;
  p=pow(a,b);

  cout << a << " to the power of " << b << " is = " << p << endl;

  //Compute 5^4
  a=5, b=4;
  p=pow(a,b);

  cout << a << " to the power of " << b << " is = " << p << endl;

    return 0;
}
